# Test-stand seed. Seeds are independent (AP): they do not sync with each other;
# agents write to every seed and merge reads (LWW). See ARCHITECTURE.md.
role: seed
node_name: seed2
datacenter: dc1
cluster:
  name: demo
listeners:
  grpc: { address: 0.0.0.0, port: 9900 }
  consul_http: { enabled: true, address: 0.0.0.0, port: 8500 }
  dns: { enabled: true, address: 0.0.0.0, port: 8600 }
  metrics: { enabled: true, address: 0.0.0.0, port: 9901 }
